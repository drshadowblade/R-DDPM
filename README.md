# R-DDPM
R-DDPM (Recurrent Denoising Diffusion Probabilistic Model) is a hybrid framework that combines recurrent neural networks with Denoising Diffusion Probabilistic Models to capture temporal dependencies in sequential data while leveraging diffusion-based generative modeling for robust sequence prediction and generation.
